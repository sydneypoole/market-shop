<view class="stepper">
  <view class="btn {{value <= min ? 'disabled' : ''}}" bindtap="onMinus">−</view>
  <text class="value">{{value}}</text>
  <view class="btn {{value >= max ? 'disabled' : ''}}" bindtap="onPlus">+</view>
</view>
